{
	"appsUsed":[
		
	],
	"createdTime":1775492239208,
	"deleted":false,
	"deployedVersion":2,
	"deploymentState":{
		"deployedAt":1775495686584,
		"deployedBy":39,
		"deployedDefinitionId":"69d3ea0646ab711125195e58",
		"status":"DEPLOYED",
		"version":2,
		"workflowVersion":4
	},
	"edges":[
		{
			"fromNodeId":"n_dbil8",
			"priority":0,
			"skip":false,
			"toNodeId":"n_LLDDu",
			"type":"next"
		}
	],
	"id":"69d3dc8fd8bff446a0c9f6e4",
	"lastModifiedBy":39,
	"lcName":"fwefweew",
	"modifiedTime":1775496542353,
	"name":"fwefweew",
	"nodes":[
		{
			"context":{
				"appName":"webhooks",
				"resourceVersion":53,
				"resourceName":"webhooks_default"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"_pubvL-1",
			"id":"n_dbil8",
			"index":1,
			"inputs":{
				"request":{
					"contentType":"application/json",
					"body":{
						"type":"object",
						"properties":{
							"inp":{
								"type":"object",
								"properties":{},
								"additionalProperties":false,
								"title":"Inp"
							}
						},
						"additionalProperties":false,
						"required":[]
					}
				}
			},
			"skip":false,
			"subTitle":"Webhook",
			"title":"New event",
			"trigger":{
				"type":"WEBHOOK"
			},
			"type":"START"
		},
		{
			"context":{
				"appName":"code_by_unifyapps",
				"resourceVersion":1676,
				"resourceName":"code_by_unifyapps_groovy",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"_pubvL-1",
			"id":"n_LLDDu",
			"index":2,
			"inputs":{
				"input":{
					"type":"object",
					"additionalProperties":false,
					"required":[],
					"properties":{
						"path":{
							"type":"string",
							"title":"Path"
						},
						"inp":{
							"type":"object",
							"properties":{},
							"additionalProperties":false,
							"title":"Inp"
						}
					}
				},
				"code":"import java.net.URI\nimport java.net.http.HttpClient\nimport java.net.http.HttpRequest\nimport java.net.http.HttpResponse\nimport org.json.JSONObject\n\nimport javax.net.ssl.*\nimport java.security.KeyStore\n\ndef RETRYABLE_ERROR_PATTERNS = [\n    \"certificate_unknown\", \"PKIX path building failed\"\n]\n\ndef SSL_ERROR_PATTERN = [\n    \"certificate_unknown\", \"PKIX path building failed\"\n]\n\ndef refreshAndSetDefaultSSLContext(int sessionTimeout) {\n    try {\n        if (sessionTimeout == -1) {\n            println(\"Default SSL Context not refreshed!\")\n            return\n        }\n\n        SSLContext sslContext = SSLContext.getInstance(\"TLS\")\n        TrustManagerFactory tmf = TrustManagerFactory.getInstance(TrustManagerFactory.getDefaultAlgorithm())\n        tmf.init((KeyStore) null)\n\n        sslContext.init(null, tmf.getTrustManagers(), new java.security.SecureRandom())\n        sslContext.getClientSessionContext().setSessionTimeout(sessionTimeout)\n\n        SSLContext.setDefault(sslContext)\n\n        println(\"Refreshed SSL context!\")\n\n    } catch (Exception eX) {\n        println(\"Failed to initialize SSL context: ${eX.message}\")\n    }\n}\n\ndef shouldRetry(Exception e, int sessionTimeout) {\n\n    def message = e.getMessage()\n\n    if (message != null) {\n\n        // SSL error → refresh context\n        for (String pattern : SSL_ERROR_PATTERN) {\n            if (message.contains(pattern)) {\n                refreshAndSetDefaultSSLContext(sessionTimeout)\n                break\n            }\n        }\n\n        // Retryable errors\n        for (String pattern : RETRYABLE_ERROR_PATTERNS) {\n            if (message.contains(pattern)) {\n                println \"Retryable error detected: ${pattern}\"\n                return true\n            }\n        }\n    }\n\n    // Check cause chain\n    def cause = e.getCause()\n    while (cause != null) {\n\n        def causeMessage = cause.getMessage()\n\n        if (causeMessage != null) {\n\n            for (String pattern : SSL_ERROR_PATTERN) {\n                if (causeMessage.contains(pattern)) {\n                    refreshAndSetDefaultSSLContext(sessionTimeout)\n                    break\n                }\n            }\n\n            for (String pattern : RETRYABLE_ERROR_PATTERNS) {\n                if (causeMessage.contains(pattern)) {\n                    println \"Retryable error detected in cause: ${pattern}\"\n                    return true\n                }\n            }\n        }\n\n        cause = cause.getCause()\n    }\n\n    return false\n}\n\n\ndef sendRequest(String url, String method, payload=null, Map headers=null){\n\n    method = method.toUpperCase()\n\n    println \"Request URL: ${url}\"\n    println \"HTTP Method: ${method}\"\n\n    def allowedHeaders = [\n        \"authorization\",\n        \"effective-of\",\n        \"metadata-context\",\n        \"rest-framework-version\",\n        \"api-token\"\n    ]\n\n    Map requestHeaders = [:]\n\n    if(headers){\n        headers.each { k,v ->\n            def key = k.toLowerCase()\n\n            if(allowedHeaders.contains(key)){\n                requestHeaders[k] = (v instanceof List) ? v[0] : v\n            }\n        }\n    }\n\n    requestHeaders[\"Content-Type\"] = \"application/json\"\n    requestHeaders[\"Accept\"] = \"application/json\"\n\n    println \"Filtered Headers: ${requestHeaders}\"\n\n    int maxRetries = 3\n    int sessionTimeout = 300  // same idea as AppProperties\n\n    Exception lastException = null\n\n    for (int attempt = 1; attempt <= maxRetries; attempt++) {\n\n        try {\n\n            println \"Attempt: ${attempt}\"\n\n            HttpClient client = HttpClient.newBuilder().build()\n\n            HttpRequest.Builder builder = HttpRequest.newBuilder()\n                    .uri(URI.create(url))\n\n            requestHeaders.each { k,v ->\n                builder.header(k, v.toString())\n            }\n\n            if([\"POST\",\"PUT\",\"PATCH\"].contains(method) && payload != null){\n\n                def jsonBody = new JSONObject(payload).toString()\n\n                println \"Preparing request body\"\n                println \"Request Body: ${jsonBody}\"\n\n                builder.method(method,\n                        HttpRequest.BodyPublishers.ofString(jsonBody))\n\n            } else {\n                builder.method(method, HttpRequest.BodyPublishers.noBody())\n            }\n\n            HttpRequest request = builder.build()\n\n            HttpResponse<String> response =\n                    client.send(request, HttpResponse.BodyHandlers.ofString())\n\n            println \"Response Code: ${response.statusCode()}\"\n            println \"Response Body: ${response.body()}\"\n\n            return [\n                status: response.statusCode(),\n                body: response.body()\n            ]\n\n        } catch(Exception e){\n\n            println \"Error on attempt ${attempt}: ${e.message}\"\n            lastException = e\n\n            if (attempt < maxRetries && shouldRetry(e, sessionTimeout)) {\n                println \"Retrying...\"\n                continue\n            } else {\n                break\n            }\n        }\n    }\n\n    return [\n        status: 500,\n        body: lastException?.message\n    ]\n}\n\n\ndef url = path\ndef method = inp?.triggerHttpMethod?.toString()\ndef payload = inp?.body\ndef headers = inp?.headers\n\ndef result = sendRequest(url, method, payload, headers)\n\nreturn result",
				"compile_static":false,
				"captureStdOutput":false,
				"parameters":{
					"path":"https://ecgy-dev2.fa.ap2.oraclecloud.com/hcmRestApi/resources/11.13.18.05/workers",
					"inp":"{{ n_dbil8.outputs.body.inp }}"
				}
			},
			"skip":false,
			"subTitle":"Code by UnifyApps",
			"title":"Execute Groovy code",
			"type":"ACTION"
		}
	],
	"ownerUserId":39,
	"schemaReferences":[
		
	],
	"settings":{
		"enableNodeLevelLogging":true,
		"enableRunLogging":true,
		"enableVariableLogging":true,
		"route":{
			"default":false,
			"tierName":"global"
		}
	},
	"standard":false,
	"version":6
}